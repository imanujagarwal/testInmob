package com.inmobi.commons.core.utilities.b;

import android.annotation.SuppressLint;
import android.content.Context;
import android.media.AudioManager;
import android.net.ConnectivityManager;
import android.net.NetworkCapabilities;
import android.net.NetworkInfo;
import android.os.Build;
import android.os.Build.VERSION;
import android.support.annotation.NonNull;
import com.inmobi.commons.a.a;
import com.inmobi.commons.core.utilities.e;
import java.util.HashMap;
import java.util.Locale;
import java.util.Map;

















public class b
{
  public b() {}
  
  @SuppressLint({"MissingPermission", "NewApi"})
  private static String b()
  {
    Object localObject;
    if ((localObject = a.b()) == null) return "";
    String str1 = "";
    String str2;
    if (e.a((Context)localObject, "root", "android.permission.ACCESS_NETWORK_STATE"))
    {


      if ((localObject = (ConnectivityManager)((Context)localObject).getSystemService("connectivity")) != null) {
        NetworkInfo localNetworkInfo;
        if ((localNetworkInfo = ((ConnectivityManager)localObject).getActiveNetworkInfo()) != null) { int i;
          if (Build.VERSION.SDK_INT < 28) {
            i = localNetworkInfo.getType();
            int j = localNetworkInfo.getSubtype();
            if (i == 0)
            {
              str2 = i + "|" + j;
            } else if (i == 1)
            {
              str2 = "1";
            } else {
              str2 = Integer.toString(i);
            }
          } else {
            NetworkCapabilities localNetworkCapabilities;
            if ((localNetworkCapabilities = i.getNetworkCapabilities(i.getActiveNetwork())).hasTransport(0)) {
              str2 = "0|" + localNetworkInfo.getSubtype();
            } else if (localNetworkCapabilities.hasTransport(1)) {
              str2 = "1";
            } else if (localNetworkCapabilities.hasTransport(2)) {
              str2 = "7";
            } else if (localNetworkCapabilities.hasTransport(3)) {
              str2 = "9";
            } else if (localNetworkCapabilities.hasTransport(4)) {
              str2 = "17";
            } else if (localNetworkCapabilities.hasTransport(5)) {
              str2 = "10";
            } else if (localNetworkCapabilities.hasTransport(6)) {
              str2 = "11";
            }
            else
            {
              str2 = "8";
            }
          }
        }
      }
    }
    return str2;
  }
  
  public static Map<String, String> a(boolean paramBoolean) {
    HashMap localHashMap = new HashMap();
    try
    {
      localHashMap.put("d-brand-name", Build.BRAND);
      localHashMap.put("d-manufacturer-name", Build.MANUFACTURER);
      localHashMap.put("d-model-name", Build.MODEL);
      
      localHashMap.put("d-nettype-raw", b());
      localHashMap.put("d-localization", 
      




































































































































































































































































































































































































































































































































































































































































































































































































































































































































































        Locale.getDefault().toString());boolean bool = paramBoolean;
      






























































































      int i = (paramBoolean = (AudioManager)paramBoolean.getSystemService("audio")).getStreamVolume(3);
      paramBoolean = paramBoolean.getStreamMaxVolume(3);localHashMap.put("d-media-volume", String.valueOf(((paramBoolean = a.b()) == null) || (bool) ? 0 : 
        i * 100 / paramBoolean));
    }
    catch (Exception paramBoolean)
    {
      b.class.getSimpleName();new StringBuilder("SDK encountered unexpected error in getting device info; ")
      
        .append(paramBoolean.getMessage());
    }
    return localHashMap;
  }
  
  public static int a() {
    String str;
    if ((str = b()).startsWith("0"))
      return 0;
    if (str.startsWith("1")) {
      return 1;
    }
    return 2;
  }
  
  public static int a(@NonNull Context paramContext)
  {
    return ((AudioManager)paramContext.getSystemService("audio")).getStreamVolume(3);
  }
}
